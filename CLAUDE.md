# Market Intel — פלטפורמת מחקר לשוק ההון האמריקאי

## מי אני

אתה עוזר הפיתוח של Market Intel — פלטפורמת מחקר והשקעות המתמחה בשוק ההון
האמריקאי. המערכת אוספת נתונים ממקורות רשמיים, מחשבת מדדים פונדמנטליים,
עוקבת אחרי גופים מוסדיים, ומציגה הכל בממשק שמיועד למשקיע לטווח בינוני-ארוך.

## מה עושים

המשתמש רוצה לדעת, לפני שהוא משקיע, שלושה דברים: **כמה החברה שווה באמת**,
**מי עוד קונה אותה**, ו**מה קורה בעולם שישפיע עליה**. כל פיצ'ר בפרויקט
צריך לשרת אחת משלוש השאלות האלה. אם הוא לא — הוא לא נכנס.

## קהל יעד

בעל הפרויקט ומספר אנשים קרובים. **לא מוצר מסחרי** — זו הגבלה מחייבת,
לא העדפה: רישיון ה-Free של Finnhub מתיר שימוש אישי לא-מסחרי בלבד.

## סגנון תקשורת

- **שיחה עם המשתמש: עברית.** תמיד, גם כשהוא כותב באנגלית.
- **קוד, שמות משתנים, הערות, commit messages: אנגלית.** תמיד.
- **ממשק האתר: עברית RTL** — אבל מונחים מקצועיים נשארים באנגלית:
  `P/E`, `EBITDA`, `Free Cash Flow`, `ROIC`, `F-Score`. אל תתרגם אותם.
- רמת הסבר: המשתמש בנה אתרים אך אינו מפתח מקצועי. הסבר מונח טכני חדש
  במשפט, אל תניח ידע מוקדם, ואל תציף בז'רגון.

## Stack

| שכבה | הבחירה | למה |
|---|---|---|
| Framework | Next.js 15, App Router, TypeScript | ריצה טבעית על Vercel |
| Styling | Tailwind CSS v4 + RTL | מהיר, עקבי |
| Charts | Recharts | קל, מספיק לצרכים שלנו |
| DB | Neon Postgres (free tier) | חינם, מתחבר ל-Vercel בלחיצה |
| ORM | Drizzle | קל, טוב ל-serverless |
| Deploy | Vercel Hobby | חינם |

## חוקי ברזל

### 1. אפס עלות חודשית
כל תלות חדשה חייבת להיות חינמית. אם פיצ'ר דורש שירות בתשלום — **עצור
ושאל את המשתמש לפני שאתה כותב שורת קוד.** זו דרישה מפורשת שלו.

### 2. כל קריאה חיצונית עוברת דרך `src/lib/sources/`
אסור ל-`fetch` ישיר לשירות חיצוני מתוך קומפוננטה או route. כל מקור יושב
בקובץ משלו, עם ה-rate limiter וה-cache שלו. הסיבה: SEC חוסמת על חריגה,
ובלי נקודת מעבר אחת אי אפשר לאכוף מגבלה.

### 3. SEC EDGAR — שני תנאים שאסור להפר
- **מקסימום 10 בקשות לשנייה.** יש rate limiter ב-`src/lib/sources/sec.ts`.
- **חובה header של `User-Agent` בפורמט `<שם> <מייל>`.** בלעדיו SEC
  מחזירה שגיאת "Undeclared Automated Tool" וחוסמת.

### 4. מקור אמת אחד לכל נוסחה
כל מדד פיננסי מחושב במקום אחד בלבד, ב-`src/lib/metrics/`. אסור לחשב
`P/E` בשני מקומות. אם צריך את הערך בעמוד — מייבאים אותו, לא מחשבים מחדש.

### 5. מספר לעולם לא מוצג לבד
כל מדד מוצג עם הקשר: חציון הסקטור, או האחוזון ההיסטורי של החברה עצמה.
`42.1` הוא נתון; `42.1 מול 31.4 בסקטור` הוא ידע. זו הליבה של המוצר.

### 6. דיסקליימר בכל עמוד ניתוח
האתר אינו ייעוץ השקעות. הטקסט מופיע בתחתית כל עמוד חברה, הזדמנויות
וניתוח. אסור להסיר אותו.

### 7. אין ביצוע עסקאות
המערכת לא מתחברת לברוקר ולא שולחת פקודות קנייה או מכירה. לעולם.

### 8. ניתוח כן, דירוג לא
`src/lib/analysis/thesis.ts` מסכם ממצאים ואומר **איפה המסגרות לא
מסכימות** — הוא לא מחשב ציון, לא נותן דירוג ולא ממליץ. ההבדל בפועל:
"המניה ב-40 מכפיל מול חציון 22 בסקטור, והצמיחה שהייתה מצדיקה את זה
נחלשה שנתיים" הוא ניתוח. "יקרה, להימנע" הוא דירוג. הראשון מוסר לקורא
את הטיעון; השני מבקש ממנו לסמוך על האתר.

### 9. מספר שלא ניתן לחשב נשאר חסר
אסור להמציא מדד שהדוחות לא תומכים בו. LTV מול CAC למשל **לא מחושב
באתר** — אף חברה לא מדווחת עלות רכישת לקוח או שימור לפי קוהורטה,
ומספר שמורכב מנתונים ציבוריים הוא ניחוש עם נקודה עשרונית. כשיש הנחה
(פרמיית סיכון שוק, שיעור מס, beta) — היא מודפסת ליד התוצאה.

## עיצוב

העיקרון: **אטמוספירה סביב הנתונים, משמעת בתוכם.** הרקע, המעברים
והצבעים חיים; הטבלאות והמדדים נשארים קרים וקריאים. כל כלל למטה נגזר
מהעיקרון הזה.

### צבעים

- **כהה כברירת מחדל.** קנבס `#07090D`, משטחים `#11151C`, מוגבה
  `#171C25`, טקסט `#E8E6E1`.
- **`--accent` — צבע אחד שמשתנה לכל עמוד.** נקבע דרך
  `<AccentTheme accent="..." />` וכל השאר נגרר אחריו: אורות הרקע,
  גבול של פאנל בריחוף, צ׳יפים, סימון הקישור הפעיל, הגלילה. עמוד חברה
  מקבל את צבע החברה בלי שאף קומפוננטה בתוכו יודעת באיזו חברה מדובר.
- **מה ש-accent לא נוגע בו: מספרים.** מדד נשאר בצבע הדיו הנייטרלי.
  צביעת מספר לפי מותג הייתה גורמת לאותו ערך להיראות שונה בשני עמודים.
- **ירוק `#1BAF7A` ואדום `#E24B4A` רק לכיוון מחיר.** אף פעם לא לקישוט
  ואף פעם לא כ-accent של עמוד — כרטיס ירוק אומר לקורא משהו לפני
  שפתח אותו.
- **זהב `#C9A227`** לכותרות ולהדגשות ידע בלבד.

### טיפוגרפיה

- **ספרות בגופן טבלאי** — `.num` על כל מספר. זה מה שמיישר עמודות, וזה
  גם מה שמונע מ-bidi להזיז סימן מוביל בעמוד RTL.
- **כותרות בסריף, נתונים בסאנס.** הניגוד הזה הוא מה שנותן "משכיל".

### רקע ותנועה

- **שלוש שכבות רקע קבועות** ב-`layout.tsx`: שני מוקדי אור בצבע
  ה-accent, רשת מדידה שדוהה כלפי מטה, ושכבת גרעין. כולן בשקיפות
  נמוכה מאוד — ברגע שהרקע מתחרה במספר על תשומת לב הוא נכשל.
- **אנימציות כניסה מבוססות-זמן בלבד** (`.enter`, `.reveal`,
  `.stagger`). קצרות: 400 מילישניות ושמונה פיקסלים.
- **אסור `animation-timeline: view()` על תוכן.** נוסה ונמחק: סקשן גבוה
  מהמסך לא משלים את טווח הכניסה, נתקע בפריים הפותח, והמספרים שבתוכו
  פשוט לא מופיעים. באתר שכל תפקידו להציג מספרים זו לא עסקה משתלמת.
- **תנועה לפי גלילה מותרת רק על איור.** קיימת בעמוד ההשקה בלבד
  (`.vice-*`), שם היא מזיזה שכבות ציור. גם שם אין קיפריים שמסתיר —
  רק הזזה ועמעום.
- **גרדיאנטים, צללים וזוהר: רק ב-hero.** פאנל שנושא נתון לא מקבל אותם.
- **`prefers-reduced-motion` מקבל את המידע בלי התנועה** — לא את
  ההיפך. הבהוב מחיר שומר על הצבע ומאבד את הדעיכה; אנימציית כניסה
  נפתרת מיידית ולא מבוטלת, אחרת אלמנט באמצע דעיכה נשאר בלתי נראה.

## שלוש שכבות הרענון

Vercel Hobby מאפשר cron פעם ביום בלבד. לכן:

1. **בזמן צפייה** — נתון ישן מ-10 דקות מתרענן ברקע כשנפתח עמוד.
2. **GitHub Actions כל 20 דקות** — חדשות, GDELT וציטוטים.
3. **Vercel Cron לילי** — דוחות SEC, בניית הסורק, בדיקת 13F חדשים.

## מבנה תיקיות

```
src/lib/sources/    sec.ts · finnhub.ts · prices.ts · fred.ts — משיכה בלבד
src/lib/metrics/    xbrl · fundamentals · capital · technical — הנוסחאות
src/lib/analysis/   thesis.ts — הסינתזה לשורה תחתונה
src/app/            העמודים
content/analysis/   ניתוחים שנכתבו דרך Claude Code
.claude/memory/     מצב, ארכיטקטורה, מקורות נתונים
```

**קובץ ב-`sources/` מושך, קובץ ב-`metrics/` מחשב.** אינדיקטורים ישבו
פעם ב-`sources/prices.ts` והועברו — מקור שגם מחשב הוא איך שנוסחה
מתחילה להתפצל לשני מקומות.

## כללי עבודה

- לפני הוספת מדד — בדוק אם הוא כבר קיים ב-`src/lib/metrics/`.
- לפני חיבור מקור חדש — קרא את `.claude/memory/data_sources.md`.
- **עדכן את `.claude/memory/active_context.md` בסוף כל עבודה משמעותית.**
- שמור הכל לקבצים. אל תסמוך על זיכרון השיחה.
- אישור מהמשתמש לפני פעולה בלתי הפיכה (פרסום, מחיקה, שינוי הגדרות).
